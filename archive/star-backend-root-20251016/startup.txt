# Azure App Service startup command for STAR Platform (Frontend + Backend)
# Flask app serves both React frontend and API endpoints

gunicorn --bind=0.0.0.0:$PORT --workers=1 --timeout=300 --chdir star_backend_flask app:app