# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: star
metadata:
    template: azd-init@1.20.0
services:
    star-backend:
        project: ./star-backend
        host: appservice
        language: python
    star-frontend:
        project: ./star-frontend
        host: appservice
        language: js
