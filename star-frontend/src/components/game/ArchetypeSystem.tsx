import React from 'react';

interface ArchetypeSystemProps {
    playerId: string;
    resonances: any[];
    setResonances: (resonances: any[]) => void;
}

export const ArchetypeSystem: React.FC<ArchetypeSystemProps> = ({ playerId, resonances, setResonances }) => {
    // Placeholder component
    return null;
};