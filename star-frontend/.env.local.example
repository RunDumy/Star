# Spotify OAuth (PKCE)
# Create a Spotify Developer App and add your redirect URI(s) there.
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=
NEXT_PUBLIC_SPOTIFY_REDIRECT_URI=http://localhost:3000/api/auth/callback/spotify

# Optional: override the frontend path used to start auth (defaults to /api/auth/spotify)
NEXT_PUBLIC_SPOTIFY_AUTH_PATH=/api/auth/spotify
